import { JoinPlatformsPipe } from './join-platforms.pipe';

describe('JoinPlatformsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinPlatformsPipe();
    expect(pipe).toBeTruthy();
  });
});
